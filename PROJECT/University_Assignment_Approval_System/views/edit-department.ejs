<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Edit Department</title>

  <style>
    :root {
      --blue:#4b6cb7;
      --bg:#f3f5f9;
    }

    * { box-sizing: border-box; }

    body {
      margin: 0;
      background: var(--bg);
      font-family: "Segoe UI", sans-serif;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      padding: 16px;
    }

    .box {
      width: 100%;
      max-width: 400px;
      background: #fff;
      padding: 24px;
      border-radius: 12px;
      box-shadow: 0 4px 14px rgba(0,0,0,0.08);
    }

    h2 {
      margin: 0 0 16px;
      font-size: 22px;
      text-align: center;
    }

    input, select {
      width: 100%;
      padding: 12px;
      margin: 8px 0;
      border-radius: 8px;
      border: 1px solid #ccc;
      font-size: 15px;
    }

    .btn {
      width: 100%;
      padding: 12px;
      margin-top: 10px;
      border: none;
      border-radius: 8px;
      background: var(--blue);
      color: #fff;
      font-weight: 700;
      cursor: pointer;
    }

    .msg {
      margin-top: 12px;
      padding: 10px;
      border-radius: 6px;
      font-weight: 700;
      text-align: center;
      font-size: 14px;
    }

    .ok { background: #d4edda; color: #155724; }
    .err { background: #f8d7da; color: #721c24; }

    .back {
      display: block;
      text-align: center;
      background: #555;
      padding: 12px;
      border-radius: 8px;
      margin-top: 12px;
      color: #fff;
      font-weight: 700;
      text-decoration: none;
    }
  </style>
</head>

<body>
  <div class="box">
    <h2>Edit Department</h2>

    <form action="/admin/departments/update/<%= department._id %>" method="POST">
      
      <input type="text" name="name" value="<%= department.name %>" required>

      <select name="type" required>
        <option value="UG"       <%= department.type === "UG" ? "selected" : "" %>>UG</option>
        <option value="PG"       <%= department.type === "PG" ? "selected" : "" %>>PG</option>
        <option value="Research" <%= department.type === "Research" ? "selected" : "" %>>Research</option>
      </select>

      <input type="text" name="address" value="<%= department.address %>" required>

      <button class="btn">Update</button>
    </form>

    <% if (success) { %>
      <div class="msg ok"><%= success %></div>
    <% } %>

    <% if (error) { %>
      <div class="msg err"><%= error %></div>
    <% } %>

    <a href="/admin/departments" class="back">Back to Department List</a>
  </div>
</body>
</html>
